{
  "name": "schema-adaptive-ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ingest customer spreadsheets with differing column layouts without calling an LLM on every file.",
  "scripts": {
    "test": "vitest run",
    "demo": "tsx src/demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
